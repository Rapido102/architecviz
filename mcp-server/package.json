{
  "name": "architectviz-mcp",
  "version": "0.1.0",
  "description": "MCP server for ArchitectViz — section-by-section extraction with idempotent merge",
  "type": "module",
  "private": true,
  "bin": {
    "architectviz-mcp": "./dist/server.js"
  },
  "scripts": {
    "sync-core": "node scripts/sync-core.mjs",
    "prebuild": "node scripts/sync-core.mjs",
    "build": "tsc",
    "predev": "node scripts/sync-core.mjs",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
